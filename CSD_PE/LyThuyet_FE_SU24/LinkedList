1) In a linked list the tail node is introduced for performance purpose only.

2) Node
_ Data: info (chứa dữ liệu)
_ Node: next (là mắt xích - con trỏ)

#linked list truy xuất ra liên kết chậm hơn array vì phải bắt đầu từ head đến tail -> giống selection sort
                 Array             LinkedList
add                On                 O(1)
delete             On                 O(1)
search(i)          O(1)               On
search(value)      On                 On


